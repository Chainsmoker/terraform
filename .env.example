DIGITALOCEAN_TOKEN=dop_v1_replace_me

# HCP Terraform — necesario para `terraform init`.
# Source este fichero antes de correr terraform:
#   set -a; source .env; set +a
TF_CLOUD_ORGANIZATION=tu-org-hcp
TF_WORKSPACE=cliente-x-prod
